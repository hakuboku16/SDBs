"""
スラッシュコマンド (cog) を集約するパッケージ

各スラッシュコマンドは 1 ファイル 1 cog として本パッケージ配下に配置されます。
`src.core.bot.SDBsBot.setup_hook` がこのパッケージを `pkgutil.iter_modules` で
走査し、全 cog を自動ロードします (要件: 「1スラッシュコマンドにつき1モジュール」)。

実 cog 実装はステップ 5 で追加されます。本ステップではパッケージ化のみ行います。
"""
