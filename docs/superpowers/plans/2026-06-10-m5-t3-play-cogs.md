# M5-T3 プレイ系 cog 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** プレイ進行を司る 3 つの cog(`/report`・`/answer`・`/progress`)を実装し、プレイ申告→お題照合→盤面開示→該当お題の公開 embed、隠し曲の当てと正解者記録、現在状況の ephemeral 表示までを `GameService` 経由で配線する。これで M5(エンドツーエンド結線)が完成する。

**Architecture:** Discord に依存しない表示文言の整形(達成お題一覧・進捗テキスト)は `GameService` のモジュール関数として TDD する。照合は既存 `SessionManager.apply_report`(`revealed_panels` 更新込み)を、盤面更新は T2 の `GameService.refresh_board` を再利用する。cog は引数受け取り・曲解決例外の ephemeral 案内・`GameService` 呼び出しに徹する薄いアダプタとし、import スモーク + 手動結合テストで検証する。

**Tech Stack:** Python 3.12 / uv / pytest / discord.py 2.4 / Pillow

**前提:** M5-T1(`GameService` 足場)と M5-T2(ライフサイクル系 cog・Discord I/O メソッド)が完了していること。

---

## 現状(着手前の事実・T2 完了時点)

- [src/bot/game_service.py](src/bot/game_service.py) の `GameService`:
  - 同期 `resolve_song(query)->Song`(`SongNotFound`/`AmbiguousSong` 送出)/ `render_current_board()->bytes` / プロパティ `session->GameSession | None` / `settings->BaseAppSettings`。
  - 非同期 `refresh_board()`(盤面・お題リストを現在状態へ更新)。
  - 属性 `session_manager: SessionManager`。
  - モジュール関数 `format_topic_list(topics)->str`、定数 `BOARD_FILENAME`。
- [src/game/session_manager.py](src/game/session_manager.py):
  - `apply_report(report: PlayReport)->list[Topic]`(新規達成を返し、`revealed_panels` を更新)。
  - `record_answer(user_id: int, song: Song)->bool`(一致時に `correct_answerers` へ記録)。
  - `remaining(now: datetime)->timedelta`。
- [src/game/models.py](src/game/models.py):`PlayReport(song, difficulty, combo, charming)` / `Difficulty`(`Easy`/`Normal`/`Hard`)/ `Topic`。
- [src/cogs/](src/cogs/) … T2 で `session_start.py` / `session_end.py` / `session_clear.py` 追加済み。

## ファイル構成

- 変更: `src/bot/game_service.py`(`format_completed_topics` / `format_progress_text` のモジュール関数を追加)
- 変更: `tests/bot/test_game_service.py`(上記 2 関数の単体テストを追加)
- 作成: `src/cogs/song_report.py`(`/report`)
- 作成: `src/cogs/answer.py`(`/answer`)
- 作成: `src/cogs/session_progress.py`(`/progress`)

---

## Task 1: 達成お題一覧・進捗テキストの整形を追加

**Files:**
- Modify: `src/bot/game_service.py`
- Test: `tests/bot/test_game_service.py`

- [ ] **Step 1: 失敗するテストを書く**

`tests/bot/test_game_service.py` の末尾へ追記する。`_topic` ヘルパは Task(T1)で定義済みのものを再利用する。

```python
from datetime import timedelta

from src.bot.game_service import format_completed_topics, format_progress_text


def test_format_completed_topics_lists_panel_and_description() -> None:
    """達成お題をパネル番号順に「パネルN: 説明」で列挙する。"""
    topics = [
        _topic(3, progress=2, required=2, completed=True),
        _topic(1, progress=1, required=1, completed=True),
    ]
    assert format_completed_topics(topics) == "パネル1: 説明1\nパネル3: 説明3"


def test_format_progress_text_shows_remaining_minutes_and_topics() -> None:
    """残り時間(分・切り捨て)とお題リストを併記する。"""
    session = _session_for_timer(datetime(2026, 6, 10, 12, 0, 0))
    session.topics = [_topic(1, progress=1, required=2, completed=False)]
    text = format_progress_text(session, timedelta(minutes=12, seconds=30))
    assert text == "残り時間: 約12分\n\nパネル1: 説明1 [1/2]"


def test_format_progress_text_clamps_negative_remaining_to_zero() -> None:
    """残り時間が負なら 0 分として表示する。"""
    session = _session_for_timer(datetime(2026, 6, 10, 12, 0, 0))
    session.topics = []
    text = format_progress_text(session, timedelta(seconds=-5))
    assert text.startswith("残り時間: 約0分")
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `APP_ENV=test uv run pytest tests/bot/test_game_service.py -k "completed_topics or progress_text" -v`
Expected: FAIL(`ImportError: cannot import name 'format_completed_topics'`)

- [ ] **Step 3: 整形関数を実装**

`src/bot/game_service.py` の `format_archive_caption` 関数の直後へモジュール関数を追加する。

```python
def format_completed_topics(topics: list[Topic]) -> str:
    """新規達成お題を公開表示用に整形する。

    Args:
        topics: 新規達成したお題群。

    Returns:
        str: パネル番号昇順に「パネルN: 説明」を改行連結した文字列。
    """
    return "\n".join(
        f"パネル{t.panel_no}: {t.description}"
        for t in sorted(topics, key=lambda t: t.panel_no)
    )


def format_progress_text(session: GameSession, remaining: timedelta) -> str:
    """進捗表示(残り時間 + お題リスト)を整形する。

    Args:
        session: 表示対象のセッション。
        remaining: 終了までの残り時間。負値は 0 分として扱う。

    Returns:
        str: 残り時間とお題リストを併記した文字列。
    """
    minutes = max(int(remaining.total_seconds() // 60), 0)
    return f"残り時間: 約{minutes}分\n\n{format_topic_list(session.topics)}"
```

`format_progress_text` は `timedelta` を使う。`src/bot/game_service.py` の import 群に `timedelta` が無ければ次へ整える(T1 で `from datetime import datetime` を入れているため、同行を置き換える)。

```python
from datetime import datetime, timedelta
```

- [ ] **Step 4: テストが通ることを確認**

Run: `APP_ENV=test uv run pytest tests/bot/test_game_service.py -v`
Expected: PASS(本タスクの 3 件を含め全件通過)

- [ ] **Step 5: コミット**

```bash
git add src/bot/game_service.py tests/bot/test_game_service.py
git commit -m "feat: 達成お題一覧と進捗テキストの整形を追加"
```

---

## Task 2: /report cog

**Files:**
- Create: `src/cogs/song_report.py`

cog は薄いアダプタのため自動テストは import スモークのみとし、実挙動は末尾の手動結合テストで検証する。

- [ ] **Step 1: cog を実装**

`src/cogs/song_report.py` を新規作成する。

```python
from __future__ import annotations

import discord
from discord import app_commands
from discord.ext import commands

from src.bot.client import GameBot
from src.bot.game_service import AmbiguousSong, SongNotFound, format_completed_topics
from src.game.models import Difficulty, PlayReport

_DIFFICULTY_CHOICES = [
    app_commands.Choice(name="Easy", value="Easy"),
    app_commands.Choice(name="Normal", value="Normal"),
    app_commands.Choice(name="Hard", value="Hard"),
]


class SongReportCog(commands.Cog):
    """プレイ申告コマンドを提供する cog。"""

    def __init__(self, bot: GameBot) -> None:
        """bot を保持して初期化する。

        Args:
            bot: コマンドを提供する GameBot。
        """
        self.bot = bot

    @app_commands.command(name="report", description="プレイ結果を申告する")
    @app_commands.describe(
        song="曲名(部分一致)",
        difficulty="難易度",
        combo="獲得コンボ数",
        charming="獲得チャーミング数",
    )
    @app_commands.choices(difficulty=_DIFFICULTY_CHOICES)
    async def report(
        self,
        interaction: discord.Interaction,
        song: str,
        difficulty: app_commands.Choice[str],
        combo: int,
        charming: int,
    ) -> None:
        """申告を全お題へ照合し、達成パネルを開示して公開表示する。

        Args:
            interaction: コマンドのインタラクション。
            song: 曲名(部分一致)。
            difficulty: 難易度の選択。
            combo: 獲得コンボ数。
            charming: 獲得チャーミング数。
        """
        game = self.bot.game
        if game.session is None:
            await interaction.response.send_message(
                "進行中のセッションがありません。", ephemeral=True
            )
            return
        try:
            resolved = game.resolve_song(song)
        except SongNotFound:
            await interaction.response.send_message(
                f"「{song}」に一致する曲が見つかりません。", ephemeral=True
            )
            return
        except AmbiguousSong as exc:
            names = "、".join(s.title for s in exc.matches[:10])
            await interaction.response.send_message(
                f"候補が複数あります。曲名を絞ってください: {names}", ephemeral=True
            )
            return
        await interaction.response.defer(ephemeral=True)
        report = PlayReport(
            song=resolved,
            difficulty=Difficulty(difficulty.value),
            combo=combo,
            charming=charming,
        )
        newly_completed = game.session_manager.apply_report(report)
        await game.refresh_board()
        if newly_completed:
            embed = discord.Embed(
                title="お題達成", description=format_completed_topics(newly_completed)
            )
            await interaction.channel.send(embed=embed)
            await interaction.followup.send("申告を反映しました。", ephemeral=True)
        else:
            await interaction.followup.send(
                "申告を反映しました。達成したお題はありません。", ephemeral=True
            )


async def setup(bot: GameBot) -> None:
    """cog を bot へ登録する。

    Args:
        bot: 登録先の GameBot。
    """
    await bot.add_cog(SongReportCog(bot))
```

- [ ] **Step 2: import スモークで検証**

Run: `uv run python -c "import src.cogs.song_report; print('ok')"`
Expected: `ok`

- [ ] **Step 3: コミット**

```bash
git add src/cogs/song_report.py
git commit -m "feat: /report cog を追加(照合・開示・該当お題の公開embed)"
```

---

## Task 3: /answer cog

**Files:**
- Create: `src/cogs/answer.py`

- [ ] **Step 1: cog を実装**

`src/cogs/answer.py` を新規作成する。

```python
from __future__ import annotations

import discord
from discord import app_commands
from discord.ext import commands

from src.bot.client import GameBot
from src.bot.game_service import AmbiguousSong, SongNotFound


class AnswerCog(commands.Cog):
    """隠し曲の当てコマンドを提供する cog。"""

    def __init__(self, bot: GameBot) -> None:
        """bot を保持して初期化する。

        Args:
            bot: コマンドを提供する GameBot。
        """
        self.bot = bot

    @app_commands.command(name="answer", description="隠し曲を当てる")
    @app_commands.describe(song="曲名(部分一致)")
    async def answer(self, interaction: discord.Interaction, song: str) -> None:
        """隠し曲と照合し、正解なら回答者を記録する(全て ephemeral)。

        Args:
            interaction: コマンドのインタラクション。
            song: 曲名(部分一致)。
        """
        game = self.bot.game
        if game.session is None:
            await interaction.response.send_message(
                "進行中のセッションがありません。", ephemeral=True
            )
            return
        try:
            resolved = game.resolve_song(song)
        except SongNotFound:
            await interaction.response.send_message(
                f"「{song}」に一致する曲が見つかりません。", ephemeral=True
            )
            return
        except AmbiguousSong as exc:
            names = "、".join(s.title for s in exc.matches[:10])
            await interaction.response.send_message(
                f"候補が複数あります。曲名を絞ってください: {names}", ephemeral=True
            )
            return
        correct = game.session_manager.record_answer(interaction.user.id, resolved)
        await interaction.response.send_message(
            "正解" if correct else "不正解", ephemeral=True
        )


async def setup(bot: GameBot) -> None:
    """cog を bot へ登録する。

    Args:
        bot: 登録先の GameBot。
    """
    await bot.add_cog(AnswerCog(bot))
```

- [ ] **Step 2: import スモークで検証**

Run: `uv run python -c "import src.cogs.answer; print('ok')"`
Expected: `ok`

- [ ] **Step 3: コミット**

```bash
git add src/cogs/answer.py
git commit -m "feat: /answer cog を追加(隠し曲照合・正解者記録)"
```

---

## Task 4: /progress cog

**Files:**
- Create: `src/cogs/session_progress.py`

- [ ] **Step 1: cog を実装**

`src/cogs/session_progress.py` を新規作成する。

```python
from __future__ import annotations

from datetime import datetime
from io import BytesIO

import discord
from discord import app_commands
from discord.ext import commands

from src.bot.client import GameBot
from src.bot.game_service import BOARD_FILENAME, format_progress_text


class SessionProgressCog(commands.Cog):
    """現在状況表示コマンドを提供する cog。"""

    def __init__(self, bot: GameBot) -> None:
        """bot を保持して初期化する。

        Args:
            bot: コマンドを提供する GameBot。
        """
        self.bot = bot

    @app_commands.command(name="progress", description="現在の盤面と進捗を表示する")
    async def progress(self, interaction: discord.Interaction) -> None:
        """現在の盤面・お題進捗・残り時間を ephemeral で表示する。

        Args:
            interaction: コマンドのインタラクション。
        """
        game = self.bot.game
        if game.session is None:
            await interaction.response.send_message(
                "進行中のセッションがありません。", ephemeral=True
            )
            return
        await interaction.response.defer(ephemeral=True)
        remaining = game.session_manager.remaining(datetime.now())
        png = game.render_current_board()
        await interaction.followup.send(
            content=format_progress_text(game.session, remaining),
            file=discord.File(BytesIO(png), filename=BOARD_FILENAME),
            ephemeral=True,
        )


async def setup(bot: GameBot) -> None:
    """cog を bot へ登録する。

    Args:
        bot: 登録先の GameBot。
    """
    await bot.add_cog(SessionProgressCog(bot))
```

- [ ] **Step 2: import スモークで検証**

Run: `uv run python -c "import src.cogs.session_progress; print('ok')"`
Expected: `ok`

- [ ] **Step 3: 全 cog ロードのスモーク + 全自動テスト回帰**

Run: `uv run python -c "import src.cogs.session_start, src.cogs.session_end, src.cogs.session_clear, src.cogs.song_report, src.cogs.answer, src.cogs.session_progress; print('ok')"`
Expected: `ok`(6 cog すべてが import できる)

Run: `APP_ENV=test uv run pytest -q`
Expected: PASS(既存 + T1/T2/T3 のテストが全件通過)

- [ ] **Step 4: コミット**

```bash
git add src/cogs/session_progress.py
git commit -m "feat: /progress cog を追加(盤面・進捗・残り時間の ephemeral 表示)"
```

---

## 手動結合テスト(実機・自動化対象外・M5 エンドツーエンド)

設計書の検証手順に沿って `.env` 設定後 `uv run python -m src.main` で確認する。

1. `/session_start panels:4` … 2×2 盤面 + ピン留めお題リストが投稿される。
2. 一致曲を必要回数 `/report song:... difficulty:Hard combo:... charming:...` … パネルが開示され盤面/お題リストが更新、該当お題が公開 embed で表示される。
3. 該当無しの `/report` … ephemeral で「達成したお題はありません。」が返る。
4. `/report song:<複数一致>` … ephemeral で候補が提示される。`/report song:<該当なし>` … ephemeral で不一致案内。
5. `/answer song:<正解>` … ephemeral で「正解」。再度 `/answer` で正解者が重複記録されない(set)。`/answer song:<不正解>` … 「不正解」。
6. `/progress` … ephemeral で現在の盤面画像 + 残り時間 + お題進捗が返る。
7. `/session_end` … アーカイブ ch に現時点の盤面 + `||曲名||` + 正解者一覧。
8. セッションが無い状態で各コマンド … ephemeral で「進行中のセッションがありません。」。

---

## このタスクで完了すること

- M5(6 cog のエンドツーエンド結線)が完了し、設計書「ゲームフロー」の全コマンドが実機で一通り動作する。
- 残るは設計書「確認したい前提」(コマンド識別子・Unicode 正規化)の実機確認のみ。

---

## Self-Review(立案者チェック結果)

- **スペック対応**: /report(部分一致解決・0/複数件案内・全お題照合・達成パネル開示・盤面/ピン更新・該当お題の公開 embed)→ Task 2 + `format_completed_topics`(Task 1)+ 既存 `apply_report`/`refresh_board`。/answer(ephemeral・部分一致照合・正解者記録・継続)→ Task 3。/progress(ephemeral・盤面・進捗・残り時間)→ Task 4 + `format_progress_text`(Task 1)。
- **プレースホルダ無し**: 各ステップに実コード・実コマンド・期待結果を記載。
- **型整合**: `format_completed_topics(topics)->str` / `format_progress_text(session, remaining)->str` を Task 1 で定義し Task 2/4 で使用。cog は `game.resolve_song`(`SongNotFound`/`AmbiguousSong`)/ `game.session_manager.apply_report`/`record_answer`/`remaining` / `game.refresh_board` / `game.render_current_board` / 定数 `BOARD_FILENAME` を、T1/T2 の定義どおり使用。`PlayReport(song, difficulty, combo, charming)` と `Difficulty("Easy"/"Normal"/"Hard")` は models と一致。
- **応答の一貫性**: /report は `defer(ephemeral=True)` 後、公開 embed は `interaction.channel.send`、申告者への確認は `followup.send(ephemeral=True)` に分離(公開ネタバレと ephemeral 確認の混在を避ける)。/answer・/progress は設計書どおり全て ephemeral。
- **テスト切り分け**: Discord 非依存の `format_completed_topics`/`format_progress_text` のみ TDD。照合は既存テスト済みの `apply_report` を再利用。cog は import スモーク + 手動結合テストで検証。
```
