# M4 盤面(board)実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 隠し曲画像へ加工(回転・グレースケール・モザイク)を施しグリッド分割した盤面を、未開示パネルは番号で被覆・開示パネルは画像スライスで表示する PNG として合成する、Discord 非依存のレンダリングエンジンを TDD で実装する。

**Architecture:** ゲームロジック層(`src/game/`)に 2 モジュールを追加する。`image_options.py` はセッション開始時の加工設定を確定(回転角の抽選)する純関数を持つ。`board.py` は Pillow で「加工パイプライン(`process_image`)」と「盤面合成(`render_board`)」を担う。どちらも Discord に依存せず、既存の `GameSession.image_options` / `revealed_panels` / `grid_size` を入力として消費する。盤面の Discord 投稿・ピン留め・`message.edit` 差し替えは Discord 層に依存するため本マイルストーンの範囲外(M5 結線で実施)。

**Tech Stack:** Python 3.12 / Pillow(画像合成)/ pytest(TDD)/ uv(依存管理)。既存の `dataclass` モデル(`ImageOptions`)を踏襲する。

---

## 設計上の前提(実装前に把握すること)

- **入力源:** 盤面は `GameSession` から `hidden_song.image_path`(`Path`)・`grid_size`(2/3/4/5)・`revealed_panels`(`set[int]`)・`image_options`(`ImageOptions`)を受け取って合成する。これらは M2/M3 で既に確定済み([src/game/models.py:115-145](../../../src/game/models.py#L115-L145))。本計画でモデルは変更しない。
- **`ImageOptions`:** `rotate: int | None`(0/90/180/270 または None)・`grayscale: bool`・`mosaic_px: int | None`。回転角の抽選は M4 の `resolve_image_options` で一度だけ行い、以後セッション中固定する(設計書「加工はセッション開始時に確定し以後固定」)。
- **基準キャンバス:** 300px 正方。グリッド辺は 2/3/4/5 で、いずれも 300 を割り切るため 1 タイルは 150/100/75/60px の正方になる。
- **加工順序:** 回転 → グレースケール → モザイク(設計書「盤面描画」節)。モザイクは「指定 px へ縮小 → 300px へ拡大」。
- **パネル番号:** 行優先(row-major)で 1..N を割り当て、`Topic.panel_no` と一致させる。`panel_no = row * grid_size + col + 1`。
- **依存追加:** `Pillow`(`uv add`)。`from PIL import Image, ImageDraw, ImageFont`。Pillow >= 10 を前提(`ImageFont.load_default()` が `FreeTypeFont` を返す)。

---

## ファイル構成

- 変更: `pyproject.toml` — `dependencies` に `pillow` を追加
- 新規: `src/game/image_options.py` — `resolve_image_options`(加工設定の確定・回転角抽選)
- 新規: `src/game/board.py` — `process_image`(加工パイプライン)・`render_board`(盤面合成)・`_draw_panel`(被覆タイル描画)
- 新規テスト: `tests/game/test_image_options.py`
- 新規テスト: `tests/game/test_board.py`

---

## Task 1: Pillow 依存の追加

**Files:**
- Modify: `pyproject.toml`(`dependencies`)

- [ ] **Step 1: Pillow を追加**

Run:
```bash
uv add pillow
```
Expected: `pyproject.toml` の `[project].dependencies` に `pillow>=...` が追記され、`uv.lock` が更新される。

- [ ] **Step 2: インポートできることを確認**

Run:
```bash
uv run python -c "from PIL import Image, ImageDraw, ImageFont; print(Image.__version__)"
```
Expected: バージョン番号(例 `11.x.x`)が表示され、エラーが出ない。

- [ ] **Step 3: コミット**

```bash
git add pyproject.toml uv.lock
git commit -m "build: add Pillow dependency for board rendering"
```

---

## Task 2: 画像加工設定の確定(resolve_image_options)

加工設定をセッション開始時に確定する純関数。回転角の抽選はここで一度だけ行う(乱数源を注入してテスト可能にする)。

**Files:**
- Create: `src/game/image_options.py`
- Test: `tests/game/test_image_options.py`

- [ ] **Step 1: 失敗するテストを書く**

`tests/game/test_image_options.py`:
```python
import random

from src.game.image_options import resolve_image_options


def test_resolve_no_rotate_yields_none() -> None:
    opts = resolve_image_options(
        rotate=False, grayscale=True, mosaic_px=90, rng=random.Random(0)
    )
    assert opts.rotate is None
    assert opts.grayscale is True
    assert opts.mosaic_px == 90


def test_resolve_rotate_picks_from_allowed_angles() -> None:
    opts = resolve_image_options(
        rotate=True, grayscale=False, mosaic_px=None, rng=random.Random(0)
    )
    assert opts.rotate in (0, 90, 180, 270)


def test_resolve_rotate_is_deterministic_for_seed() -> None:
    a = resolve_image_options(
        rotate=True, grayscale=False, mosaic_px=None, rng=random.Random(123)
    )
    b = resolve_image_options(
        rotate=True, grayscale=False, mosaic_px=None, rng=random.Random(123)
    )
    assert a.rotate == b.rotate
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `uv run pytest tests/game/test_image_options.py -v`
Expected: FAIL(`ModuleNotFoundError: No module named 'src.game.image_options'`)

- [ ] **Step 3: 最小実装を書く**

`src/game/image_options.py`:
```python
from __future__ import annotations

import random

from src.game.models import ImageOptions

_ROTATE_ANGLES = (0, 90, 180, 270)


def resolve_image_options(
    *,
    rotate: bool,
    grayscale: bool,
    mosaic_px: int | None,
    rng: random.Random,
) -> ImageOptions:
    """セッション開始時の画像加工設定を確定する。

    回転角はここで一度だけ抽選し、以後セッション中固定する。

    Args:
        rotate: 回転を行うか。True なら 0/90/180/270 から 1 つ抽選する。
        grayscale: グレースケール化するか。
        mosaic_px: モザイクの縮小先 px。None ならモザイクなし。
        rng: 回転角抽選に使う乱数源(テストで注入可能にするため引数化)。

    Returns:
        ImageOptions: 解決済みの加工設定。
    """
    angle = rng.choice(_ROTATE_ANGLES) if rotate else None
    return ImageOptions(rotate=angle, grayscale=grayscale, mosaic_px=mosaic_px)
```

- [ ] **Step 4: テストが通ることを確認**

Run: `uv run pytest tests/game/test_image_options.py -v`
Expected: PASS(3 passed)

- [ ] **Step 5: コミット**

```bash
git add src/game/image_options.py tests/game/test_image_options.py
git commit -m "feat: 画像加工設定の確定(回転角抽選)を追加"
```

---

## Task 3: 加工パイプライン(process_image)

300px 正方の画像へ「回転 → グレースケール → モザイク」の順で加工を適用する。盤面合成から切り出した単一責務の関数として実装し、加工の正しさをここで検証する。

**Files:**
- Create: `src/game/board.py`
- Test: `tests/game/test_board.py`

- [ ] **Step 1: 失敗するテストを書く**

`tests/game/test_board.py`:
```python
from PIL import Image

from src.game.board import process_image
from src.game.models import ImageOptions


def _opts(
    *, rotate: int | None = None, grayscale: bool = False, mosaic_px: int | None = None
) -> ImageOptions:
    return ImageOptions(rotate=rotate, grayscale=grayscale, mosaic_px=mosaic_px)


def _half_image() -> Image.Image:
    # 上半分=赤 / 下半分=青の非対称画像。回転・スライスの差を検出できる。
    img = Image.new("RGB", (300, 300), (255, 0, 0))
    img.paste((0, 0, 255), (0, 150, 300, 300))
    return img


def test_process_identity_keeps_pixels() -> None:
    out = process_image(_half_image(), _opts())
    assert out.getpixel((10, 10)) == (255, 0, 0)
    assert out.getpixel((10, 290)) == (0, 0, 255)


def test_process_grayscale_makes_channels_equal() -> None:
    r, g, b = process_image(_half_image(), _opts(grayscale=True)).getpixel((10, 10))
    assert r == g == b


def test_process_rotate_180_flips_top_and_bottom() -> None:
    out = process_image(_half_image(), _opts(rotate=180))
    assert out.getpixel((10, 10)) == (0, 0, 255)
    assert out.getpixel((10, 290)) == (255, 0, 0)


def test_process_mosaic_creates_uniform_blocks() -> None:
    # 横グラデーションをモザイク化すると 1 ブロック内が同色になる。
    grad = Image.new("RGB", (300, 300))
    for x in range(300):
        grad.paste((x // 2, 0, 0), (x, 0, x + 1, 300))
    out = process_image(grad, _opts(mosaic_px=30))
    block = 300 // 30  # 10px 角のブロック
    assert out.getpixel((0, 0)) == out.getpixel((block - 1, block - 1))
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `uv run pytest tests/game/test_board.py -v`
Expected: FAIL(`ModuleNotFoundError: No module named 'src.game.board'`)

- [ ] **Step 3: 最小実装を書く**

`src/game/board.py`:
```python
from __future__ import annotations

from PIL import Image

from src.game.models import ImageOptions

CANVAS_SIZE = 300

_ROTATE_OPS = {
    90: Image.Transpose.ROTATE_90,
    180: Image.Transpose.ROTATE_180,
    270: Image.Transpose.ROTATE_270,
}


def process_image(image: Image.Image, options: ImageOptions) -> Image.Image:
    """基準画像へ加工を回転→グレースケール→モザイクの順で適用する。

    Args:
        image: 300px 正方の RGB 画像。
        options: 適用する加工設定(解決済み)。

    Returns:
        Image.Image: 加工後の 300px 正方 RGB 画像。
    """
    result = image
    # 回転は 90 度単位のため transpose で補間なしに行い、テストの厳密一致を保つ。
    if options.rotate:
        result = result.transpose(_ROTATE_OPS[options.rotate])
    if options.grayscale:
        result = result.convert("L").convert("RGB")
    if options.mosaic_px:
        small = result.resize(
            (options.mosaic_px, options.mosaic_px), Image.Resampling.BILINEAR
        )
        result = small.resize((CANVAS_SIZE, CANVAS_SIZE), Image.Resampling.NEAREST)
    return result
```

- [ ] **Step 4: テストが通ることを確認**

Run: `uv run pytest tests/game/test_board.py -v`
Expected: PASS(4 passed)

- [ ] **Step 5: コミット**

```bash
git add src/game/board.py tests/game/test_board.py
git commit -m "feat: 盤面加工パイプライン(回転・グレースケール・モザイク)を追加"
```

---

## Task 4: 盤面合成(render_board)

加工後画像をグリッド分割し、未開示パネルは番号入りの不透明パネルで被覆、開示パネルは画像スライスを表示して PNG バイト列を返す。

**Files:**
- Modify: `src/game/board.py`
- Test: `tests/game/test_board.py`

- [ ] **Step 1: 失敗するテストを書く(`tests/game/test_board.py` の末尾へ追記)**

```python
from io import BytesIO
from pathlib import Path

from src.game.board import _PANEL_FILL, render_board


def _png_path(tmp_path: Path, img: Image.Image) -> Path:
    path = tmp_path / "hidden.png"
    img.save(path, format="PNG")
    return path


def test_render_returns_300px_png(tmp_path: Path) -> None:
    path = _png_path(tmp_path, _half_image())
    data = render_board(path, grid_size=3, revealed_panels=set(), options=_opts())
    out = Image.open(BytesIO(data))
    assert out.format == "PNG"
    assert out.size == (300, 300)


def test_render_covered_tile_differs_from_revealed(tmp_path: Path) -> None:
    path = _png_path(tmp_path, _half_image())
    covered = Image.open(
        BytesIO(render_board(path, grid_size=2, revealed_panels=set(), options=_opts()))
    ).convert("RGB")
    revealed = Image.open(
        BytesIO(render_board(path, grid_size=2, revealed_panels={1}, options=_opts()))
    ).convert("RGB")
    # パネル1=左上タイル(0,0)-(150,150)の中心 (40,40)。
    assert covered.getpixel((40, 40)) != revealed.getpixel((40, 40))
    assert revealed.getpixel((40, 40)) == (255, 0, 0)


def test_render_reveals_correct_panel_by_number(tmp_path: Path) -> None:
    # 2x2。パネル3=左下、パネル4=右下。下半分は青。
    path = _png_path(tmp_path, _half_image())
    board = Image.open(
        BytesIO(render_board(path, grid_size=2, revealed_panels={3}, options=_opts()))
    ).convert("RGB")
    assert board.getpixel((40, 220)) == (0, 0, 255)   # パネル3=開示(画像の青)
    assert board.getpixel((220, 220)) == _PANEL_FILL   # パネル4=被覆(パネル色)
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `uv run pytest tests/game/test_board.py -v`
Expected: FAIL(`ImportError: cannot import name '_PANEL_FILL'` または `render_board`)

- [ ] **Step 3: 実装を追記する(`src/game/board.py`)**

import 行を差し替え:
```python
from __future__ import annotations

from io import BytesIO
from pathlib import Path

from PIL import Image, ImageDraw, ImageFont

from src.game.models import ImageOptions
```

定数 `CANVAS_SIZE` の直後に追記:
```python
_PANEL_FILL = (40, 40, 40)
_NUMBER_FILL = (255, 255, 255)
```

`process_image` の後ろに追記:
```python
def render_board(
    image_path: Path,
    *,
    grid_size: int,
    revealed_panels: set[int],
    options: ImageOptions,
) -> bytes:
    """隠し曲画像から現在の盤面を合成し PNG バイト列を返す。

    未開示パネルは番号入りの不透明パネルで被覆し、開示済みパネルは
    加工後画像のスライスを表示する。

    Args:
        image_path: 隠し曲のジャケット画像パス。
        grid_size: 盤面の一辺のパネル数(2/3/4/5)。
        revealed_panels: 開示済みパネル番号(1..grid_size**2)の集合。
        options: 画像加工設定(解決済み)。

    Returns:
        bytes: PNG エンコードした盤面画像。
    """
    with Image.open(image_path) as raw:
        base = raw.convert("RGB").resize((CANVAS_SIZE, CANVAS_SIZE))
    processed = process_image(base, options)

    tile = CANVAS_SIZE // grid_size
    board = Image.new("RGB", (CANVAS_SIZE, CANVAS_SIZE))
    draw = ImageDraw.Draw(board)
    font = ImageFont.load_default()

    for row in range(grid_size):
        for col in range(grid_size):
            panel_no = row * grid_size + col + 1
            box = (col * tile, row * tile, (col + 1) * tile, (row + 1) * tile)
            if panel_no in revealed_panels:
                board.paste(processed.crop(box), (box[0], box[1]))
            else:
                _draw_panel(draw, box, panel_no, font)

    buffer = BytesIO()
    board.save(buffer, format="PNG")
    return buffer.getvalue()


def _draw_panel(
    draw: ImageDraw.ImageDraw,
    box: tuple[int, int, int, int],
    number: int,
    font: ImageFont.FreeTypeFont,
) -> None:
    """未開示タイルへ不透明パネルと中央寄せの番号を描く。

    Args:
        draw: 描画対象の ImageDraw。
        box: タイルの矩形 (left, top, right, bottom)。
        number: 表示するパネル番号。
        font: 番号描画に使うフォント。
    """
    left, top, right, bottom = box
    draw.rectangle(box, fill=_PANEL_FILL)
    text = str(number)
    text_box = draw.textbbox((0, 0), text, font=font)
    text_w = text_box[2] - text_box[0]
    text_h = text_box[3] - text_box[1]
    cx = left + (right - left - text_w) / 2
    cy = top + (bottom - top - text_h) / 2
    draw.text((cx, cy), text, fill=_NUMBER_FILL, font=font)
```

- [ ] **Step 4: テストが通ることを確認**

Run: `uv run pytest tests/game/test_board.py -v`
Expected: PASS(7 passed: process_image 4 + render_board 3)

- [ ] **Step 5: 全テストの回帰確認**

Run: `uv run pytest`
Expected: 既存テストを含め全て PASS

- [ ] **Step 6: コミット**

```bash
git add src/game/board.py tests/game/test_board.py
git commit -m "feat: 盤面合成(被覆パネル・番号・開示スライス・PNG出力)を追加"
```

---

## このマイルストーンで実装しないこと(M5 へ委譲)

設計書では M4 に「セッションへの組込み(盤面投稿・ピン・達成時開示)」が含まれるが、これらは Discord 層(`src/bot/`・`src/cogs/`)に依存し、現状リポジトリに未実装のため本計画の範囲外とする(スコープ確認済み)。M5「結線」で以下を行う:

- `/session_start` で `resolve_image_options` を呼び加工設定を確定し、`render_board` の出力を盤面メッセージとして投稿・ピン留め
- `/report` で新規達成時に `render_board` を再合成し `message.edit` で差し替え
- `/session_end` で「その時点の盤面画像」をアーカイブ投稿

本計画の成果物(`process_image` / `render_board` / `resolve_image_options`)はいずれも純関数で、M5 の cog からそのまま呼び出せる。

---

## 自己レビュー結果

- **設計書「盤面描画」節カバレッジ:** 入力(画像・グリッド辺・開示集合・加工設定)→ Task 4 `render_board` 引数 / 300px キャンバス → `CANVAS_SIZE` / 加工順(回転→グレースケール→モザイク)→ Task 3 `process_image` / モザイク(縮小→拡大)→ Task 3 / グリッド分割・被覆パネル+番号・開示スライス → Task 4 / パネル番号 1..N=panel_no → Task 4 `panel_no` 算出 / PNG バイト列返却 → Task 4 / 回転角の開始時抽選・以後固定 → Task 2 `resolve_image_options`。すべて対応タスクあり。
- **M4 検証項目「加工・合成・開示」:** 加工=Task 3 の grayscale/rotate/mosaic テスト、合成=Task 4 のサイズ/PNG テスト、開示=Task 4 の被覆 vs 開示・パネル番号テストで自動検証。
- **型整合:** `ImageOptions(rotate, grayscale, mosaic_px)` は全タスクで同一シグネチャ。`process_image(image, options) -> Image.Image` と `render_board(image_path, *, grid_size, revealed_panels, options) -> bytes` は定義と呼び出しで一致。`_PANEL_FILL` は Task 4 で定義しテストで参照。
- **プレースホルダ:** なし(全ステップに実コード/実コマンドを記載)。
